import { StyleSheet, Text, View } from "react-native";
import React from "react";

const stockContext = () => {
  return (
    <View>
      <Text>stockContext</Text>
    </View>
  );
};

export default stockContext;

const styles = StyleSheet.create({});
